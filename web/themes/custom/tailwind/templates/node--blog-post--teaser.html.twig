{#
/**
 * @file
 * Theme override to display a blog post node in teaser view mode.
 *
 * Available variables:
 * - node: The node entity with limited access to object properties and methods.
 * - label: The title of the node.
 * - content: All node items.
 * - url: Direct URL of the current node.
 *
 * @see template_preprocess_node()
 */
#}
{{ include('tailwind:block-card', {
  url: url,
  title: label,
  image: node.field_image.entity ? content.field_image : null,
  date_iso: node.created.value ? node.created.value|date('Y-m-d') : null,
  date_formatted: node.created.value ? node.created.value|date('F j, Y') : null,
  body: content.body,
  read_more_text: 'Read more'|t
}) }}
