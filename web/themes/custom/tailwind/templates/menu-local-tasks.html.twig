{#
/**
 * @file
 * Theme override to display primary and secondary local tasks.
 *
 * Available variables:
 * - primary: HTML list items representing primary tasks.
 * - secondary: HTML list items representing secondary tasks.
 *
 * Each item in these variables (primary and secondary) can be individually
 * themed in menu-local-task.html.twig.
 */
#}
{% if primary %}
  <h2 class="visually-hidden">{{ 'Primary tabs'|t }}</h2>
  <nav role="navigation" aria-label="{{ 'Primary tabs'|t }}" class="mb-6">
    <ul{{ attributes.addClass('tabs', 'primary') }}>{{ primary }}</ul>
  </nav>
{% endif %}
{% if secondary %}
  <h2 class="visually-hidden">{{ 'Secondary tabs'|t }}</h2>
  <nav role="navigation" aria-label="{{ 'Secondary tabs'|t }}" class="mb-6">
    <ul{{ attributes.addClass('tabs', 'secondary') }}>{{ secondary }}</ul>
  </nav>
{% endif %}

