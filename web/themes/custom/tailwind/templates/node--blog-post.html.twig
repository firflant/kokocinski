{#
/**
 * @file
 * Theme override for a blog post node in default view mode.
 *
 * Available variables:
 * - node: The node entity.
 * - content: All node items. Use {{ content }} to print them all.
 *
 * @see template_preprocess_node()
 */
#}
{% if node.field_image.entity %}
  {{ include('tailwind:content-image', { content: content.field_image }) }}
{% endif %}

{{ include('tailwind:rich-text', { content: content|without('field_image') }) }}

{{ include('tailwind:authored-footer', { directory: directory }) }}
