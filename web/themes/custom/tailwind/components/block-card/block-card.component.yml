$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: Block Card
status: stable
description: 'Card block with optional image, title, meta, excerpt and read-more link'
props:
  type: object
  required:
    - url
  properties:
    url:
      type: string
      title: 'Link URL'
    card_title:
      type: string
      title: 'Card title'
    image:
      type: string
      title: 'Rendered image (above title)'
    meta:
      type: string
      title: 'Meta line (e.g. read time)'
    body:
      type: string
      title: 'Rendered excerpt/body content'
    read_more_text:
      type: string
      title: '"Read more" link label'
