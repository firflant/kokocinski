page_analytics.settings:
  type: config_object
  label: "Page analytics settings"
  mapping:
    sampling_rate:
      type: integer
      label: "Sampling rate (1 in N)"
    retention_days:
      type: integer
      label: "Retention (days)"
    exclude_authenticated_users:
      type: boolean
      label: "Exclude logged-in users"
